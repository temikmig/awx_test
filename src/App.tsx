import { Layout } from "./layout/Layout";
import { PayoutForm } from "./components/PayoutForm";

export const App = () => {
  return (
    <Layout>
      <PayoutForm />
    </Layout>
  );
};
